name: On push to main

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
  build:
    uses: Stamper-Labs/core-flows/.github/workflows/yarn-nodejs-build-pipe.yml@v1.11.1
    with:
      gh_release_mode: "yes"
    secrets: inherit

  release:
    uses: Stamper-Labs/core-flows/.github/workflows/dockerize-pipe.yml@v1.11.1
    secrets: inherit
