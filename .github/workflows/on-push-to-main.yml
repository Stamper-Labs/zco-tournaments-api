name: On push to main

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
  build:
    uses: Stamper-Labs/core-flows/.github/workflows/yarn-nestjs-build.yml@v1.9.2
    with:
      gh_release_mode: "yes"
    secrets: inherit