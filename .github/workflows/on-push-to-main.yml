name: On push to main

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
  build-release:
    uses: Stamper-Labs/core-flows/.github/workflows/yarn-nestjs-build.yml@v1.9.1
    with:
      gh_release_mode: "yes"
    secrets: inherit