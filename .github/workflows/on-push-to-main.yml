name: On push to main

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
  CI:
    uses: Stamper-Labs/core-flows/.github/workflows/yarn-nodejs-wf.yml@v3.0.1
    secrets: inherit
    with:
      mode: "distribute"